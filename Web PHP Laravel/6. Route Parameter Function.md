# 🔀 Route Parameter Function in Controller (Part V)

ផ្នែកនេះពន្យល់អំពីការបញ្ជូន ទិន្នន័យតាម **Route Parameters** (`empid`, `empname`, `position`) ទៅកាន់ **Controller Method** និងបង្ហាញលទ្ធផលនៅលើ **View**។

### 🔄 លំហូរការងារ (Data Flow):
```text
Employee 1 (Click Link)
          ↓
Route with Parameters (/employee/detail/101/John/Manager)
          ↓
EmployeeController@detail
          ↓
resources/views/employee/detail.blade.php
          ↓
Display: 
  - Employee ID: 101
  - Name: John
  - Position: Manager
```

---

## 📌 Step 24 — EmployeeController

**ទីតាំងឯកសារ:** `app/Http/Controllers/EmployeeController.php`

```php
<?php

namespace App\Http\Controllers;

class EmployeeController extends Controller
{
    public function home()
    {
        return view('home');
    }

    public function detail($empid, $empname, $position)
    {
        return view('employee.detail', [
            'empid' => $empid,
            'empname' => $empname,
            'position' => $position
        ]);
    }
}
```

> **ចំណាំ:** Method `detail()` ទទួល 3 Parameters (`$empid`, `$empname`, `$position`) ហើយបញ្ជូនបន្តទៅ View តាមរយៈ Array។

---

## 📌 Step 25 — Employee Routes

**ទីតាំងឯកសារ:** `routes/web.php`

ធ្វើការ Import Controller និងកំណត់ Named Routes:

```php
use App\Http\Controllers\EmployeeController;

Route::get('/employee', [EmployeeController::class, 'home'])
    ->name('employee.home');

Route::get(
    '/employee/detail/{empid}/{empname}/{position}',
    [EmployeeController::class, 'detail']
)->name('employee.detail');
```

> **ចំណាំ:** Laravel បញ្ជូនតម្លៃពី URL Parameters (`{empid}`, `{empname}`, `{position}`) ចូលទៅ Parameter របស់ Controller Method ដោយស្វ័យប្រវត្តិ។

---

## 📌 Step 26 — Design home.blade.php

**ទីតាំងឯកសារ:** `resources/views/home.blade.php`

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Employee Information</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            background: #eaf5ff;
        }

        .container {
            width: 400px;
            margin: 50px auto;
        }

        h2 {
            color: #17429b;
        }

        .employee-card {
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 12px #ccc;
        }

        .employee {
            display: block;
            background: #2563eb;
            color: white;
            padding: 12px;
            margin: 10px 0;
            border-radius: 5px;
            text-decoration: none;
        }

        .employee:hover {
            background: #174ec7;
        }
    </style>
</head>

<body>

<div class="container">

    <h2>LIST EMPLOYEE INFORMATION</h2>

    <div class="employee-card">

        <a
            class="employee"
            href="{{ route('employee.detail', [
                'empid' => 101,
                'empname' => 'John',
                'position' => 'Manager'
            ]) }}"
        >
            Employee 1
        </a>

        <a
            class="employee"
            href="{{ route('employee.detail', [
                'empid' => 102,
                'empname' => 'Dara',
                'position' => 'Developer'
            ]) }}"
        >
            Employee 2
        </a>

        <a
            class="employee"
            href="{{ route('employee.detail', [
                'empid' => 103,
                'empname' => 'Sokha',
                'position' => 'Designer'
            ]) }}"
        >
            Employee 3
        </a>

        <a
            class="employee"
            href="{{ route('employee.detail', [
                'empid' => 104,
                'empname' => 'Vanna',
                'position' => 'Accountant'
            ]) }}"
        >
            Employee 4
        </a>

    </div>

</div>

</body>
</html>
```

---

## 📌 Step 27 — Design detail.blade.php

**ទីតាំងឯកសារ:** `resources/views/employee/detail.blade.php`

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Employee Detail</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            background: #eaf5ff;
        }

        .container {
            width: 500px;
            margin: 50px auto;
            text-align: center;
        }

        h2 {
            color: #17429b;
        }

        table {
            width: 100%;
            background: white;
            border-collapse: collapse;
            box-shadow: 0 4px 12px #ccc;
        }

        td {
            padding: 15px;
            border: 1px solid #ddd;
        }

        td:first-child {
            background: #2563eb;
            color: white;
            font-weight: bold;
        }

        .btn {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 25px;
            background: #16a34a;
            color: white;
            text-decoration: none;
            border-radius: 5px;
        }
    </style>
</head>

<body>

<div class="container">

    <h2>EMPLOYEE DETAIL</h2>

    <table>
        <tr>
            <td>Employee ID</td>
            <td>{{ $empid }}</td>
        </tr>
        <tr>
            <td>Name</td>
            <td>{{ $empname }}</td>
        </tr>
        <tr>
            <td>Position</td>
            <td>{{ $position }}</td>
        </tr>
    </table>

    <a href="{{ route('employee.home') }}" class="btn">
        Back Home
    </a>

</div>

</body>
</html>
```

---

## 📌 Step 28 — Test Employee Application

1. ដំណើរការ Server:
   ```bash
   php artisan serve
   ```
2. បើក Browser ចូលទៅកាន់: `http://127.0.0.1:8000/employee`
   - ទំព័រនឹងបង្ហាញ **LIST EMPLOYEE INFORMATION** (Employee 1, 2, 3, 4)
3. ចុចលើ **Employee 1**:
   - URL នឹងផ្លាស់ប្តូរទៅ: `http://127.0.0.1:8000/employee/detail/101/John/Manager`
   - ទំព័រនឹងបង្ហាញព័ត៌មានលម្អិតក្នុង Table:
     - **Employee ID:** 101
     - **Name:** John
     - **Position:** Manager
   - ព្រមទាំងមាន Button **Back Home** ដើម្បីត្រឡប់ទៅកាន់ទំព័រដើមវិញ។
